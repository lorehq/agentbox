#!/usr/bin/env node
// TODO: Analyze the user's prompt for tasks that would benefit from delegation
// If the prompt mentions UI changes, visual verification, or parallelizable work,
// emit a nudge suggesting cloud agent delegation.
process.exit(0);
